/*
Splitting a String

Create a string named colors with the value "red,green,blue,yellow". Use the split method to convert the string into an array of colors and log the result to the console.
*/

let colors = "red,green,blue,yellow";

let words = colors.split(",");

console.log(words);