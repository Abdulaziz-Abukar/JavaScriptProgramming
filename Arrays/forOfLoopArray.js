/*
For-of Loop

Create an array named animals with the values ["cat", "dog", "rabbit"]. Use a for-of loop to log each animal to the console. 
*/

const animals = ["cat", "dog", "rabbit", "mouse", "racoon", "elephant", "lion"];

for (animal of animals) {
    console.log(animal);
}

