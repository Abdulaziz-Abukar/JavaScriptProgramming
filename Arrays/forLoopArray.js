/*
For Loop

Create an array named colors with the values ["red", "green", "blue"]. Use a for loop to log each color to the console.
*/

const colors = ["red", "green", "blue", "purple"];

for (let i = 0; i < colors.length; i++) {
    console.log(colors[i]);
}

