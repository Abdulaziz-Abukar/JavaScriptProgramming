/*
Basic Array Sorting

Create an array named letters with the values ["d", "b", "a", "c"]. 
Use the sort method to sort the array in alphabetical order and log the sorted array to the console.
*/

var letters = ["d", "b", "a", "c"];

letters.sort();

console.log(letters);