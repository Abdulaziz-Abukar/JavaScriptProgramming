/*
Using indexOf

Create an array named colors with the values ["red", "green", "blue", "yellow"]. 
Use the indexOf method to find the index of the color "blue" and log it to the console.
*/

const colors = ["red", "green", "blue", "yellow"];

console.log(colors.indexOf("blue"));