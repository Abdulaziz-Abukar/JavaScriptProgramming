/* Function Expression

Create a function expression that calculates the square of a number. 
Assign it to a variable named square and call it with different arguments, logging the results to the console. */

const square = function(num) {
    return num ** 2;
}

console.log(square(10))

