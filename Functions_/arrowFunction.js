/* Arrow Function

Create an arrow function named multiply that takes two parameters and returns their product. Call the function with different arguments and log the results to the console.
*/

const multiply = (num1, num2) => {
    return num1 * num2;
}

console.log(multiply(5, 5));