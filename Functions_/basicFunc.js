/* Basic Function

Create a function named add that takes two parameters and returns their sum. Call the function with different arguments and log the results to the console. */

function add(num1, num2) {
    return num1 + num2;
}

console.log(add(5, 5))