/*
Logical AND and OR Operators

Create variables isSunny and isWarm with boolean values. Use logical operators to check if it is either sunny or warm. Log "Great weather" if at least one condition is true.
*/

var isSunny = false;
var isWarm = false;

if (isSunny == true || isWarm == true) {
    console.log("Great Weather.")
} else {
    console.log("We need better weather.")
}