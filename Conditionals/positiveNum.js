/* Check if a Number is Positive

Create a variable num with any integer value. Use an if statement to check if the number is positive. If it is, log "The number is positive" to the console. */

var num = 10;

if (num >= 0) {
    console.log(`the number ${num} is a positive number`);
} 
else {
    console.log(`the number ${num} is a negative`)
}