/* Check Age for Voting

Create a variable age with an integer value. Use an if else statement to check if the person is eligible to vote (age 18 or older). 
Log "You are eligible to vote" or "You are not eligible to vote" to the console accordingly. */

var age = 10;

if (age >= 18) {
    console.log("You are allowed to vote.");
} else {
    console.log("You are underage");
}