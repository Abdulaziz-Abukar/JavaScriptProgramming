/* Create a variable password and assign it a string value. Use an if else statement to check if the password length is at least 8 characters.
 If it is, log "Password is strong" to the console; otherwise, log "Password is too short". */

 var password = "P"

 if (password.length < 8) {
    console.log("Password is too short.")
 } 
 else {
    console.log("Password is strong.")
 }