/*
Finding Maximum and Minimum

Create an array of numbers [10, 20, 5, 15, 30]. Use the Math.max and Math.min methods to find the largest and smallest numbers in the array. Log the results to the console.
*/

let nums = [10, 20, 5, 15, 30];
console.log(Math.max(...nums)); // 30

console.log(Math.min(...nums)); // 5
