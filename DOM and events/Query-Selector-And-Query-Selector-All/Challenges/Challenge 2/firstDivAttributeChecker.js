// Select the first <div> and log whether it has a "data-type" attribute using .hasAttribute()

const firstBox = document.querySelector(".container");

console.log(firstBox.hasAttribute("data-box-type"));