// Create a new element that contains nested HTML (like a div with a span inside) using innerHTML.

const body = document.querySelector("body");

body.innerHTML = "<div>Hello<span> People!</span></div>";

